the textbox on the left is the input textbox, which is used to paste in a chunk of text with emails contained within

the textbox on the right is the output textbox, which shows the processed results 

there should be a vertical set of buttons in the middle of the page that processes the input from the left box and outputs the results to the right textbox

the buttons in the middle are in the list below. each button corresponds to an item, and should perform the action listed:

- convert the input and render the output to just emails, one per line
- convert the input and render the output to just emails, all on one line separated by commas
- convert the input and render the output to just emails, all on one line separated by semicolons
- convert the input and render the output to just emails, just the part before domains, one per line
- convert the input and render the output to just emails, just the part before domains, all on one line, separated by commas
- convert the input and render the output to just emails, just the part before domains, all on one line, separated by semicolons
- convert the input and render the output to url for teams chat, which looks like https://teams.microsoft.com/l/chat/0/0?users=<email1>,<email2>,<email3>
- convert the input and render the output to url for slack chat
- convert the input and render the output to url for google workspace chat
- button to take the existing output textbox and sort the rows alphabetically
- button to copy right side to the user's clipboard
